meta {
  name: Revoke Session
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/auth/sessions/:id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:path {
  id: <session-id>
}
